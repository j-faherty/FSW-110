const firstNames = ['derik', 'sophie', 'naomi']
const lastNames = ['ryker', 'bloom','tucker']
const ages = [18, 22, 40]

function customers () {
    firstNames.concat(lastNames);
    firstNames.toUpperCase();
}

console.log(customers)